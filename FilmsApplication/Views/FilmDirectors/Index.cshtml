@model IEnumerable<FilmsApplication.Models.FilmDirector>

@{
    ViewData["Title"] = "Index";
}

<h1>List of films for @ViewBag.DirectorName</h1>

<p>
    <a asp-action="Create" asp-route-id=@ViewBag.DirectorId asp-route-name=@ViewBag.DirectorName>Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Film)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Film.FilmName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FilmDirectorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FilmDirectorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FilmDirectorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
